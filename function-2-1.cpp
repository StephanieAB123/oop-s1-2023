#include <iostream> 
#include <string>
using namespace std; 

//given a positive integer decimal number(i.e. base-10), 
//write a function that transforms it into binary (base -2), 
//and prints out the result.
// you may assume that the string contains at least 1 and no more 
// than 9 digits. 
// each digit in the string is 0-9. 
// examples of input strings: "0", "789" or "123456789"
// the output should only include the binary digits 
// (i.e 0-1) followed by a new line. 

// example outputs: "1101", "101", "1110111"

//hint: you might need to convert a string to integer. have a look at the stoi(string) in the c++11 string library. if you need to compile with c++11, include the compilation
//flag -std=c++11 in your compile command. 



void print_binary_str(std::string decimal_number){

    int decimal = stoi(decimal_number);

    x

}